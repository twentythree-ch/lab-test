version: '3.9'

services:
  hello-world:
    image: nginx:alpine
    ports:
      - "80:80"
    networks:
      - external-services
    # secrets:
    #   - my_secret
    restart: unless-stopped

# secrets:
#   my_secret:
#     file: ./secrets/my_secret.txt

networks:
  external-services:
    external: true