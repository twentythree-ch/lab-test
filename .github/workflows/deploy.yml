name: Deploy

on:
  push:
    branches: [develop, main]

jobs:
  deploy:
    uses: twentythree-ch/github-workflows/.github/workflows/deploy-to-portainer.yml@main
    with:
      stack_name: ${{ github.event.repository.name }}  # Uses repo name as stack name
    secrets:
      portainer_url: ${{ secrets.PORTAINER_URL }}
      portainer_token: ${{ secrets.PORTAINER_TOKEN }}
      portainer_endpoint_id: ${{ secrets.PORTAINER_ENDPOINT_ID }}
      cloudflare_client_id: ${{ secrets.CF_CLIENT_ID }}
      cloudflare_client_secret: ${{ secrets.CF_CLIENT_SECRET }}
      github_secret: ${{ secrets.GH_BOTPROTECTION_SECRET }}